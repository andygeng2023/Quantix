const priceEl=document.getElementById('priceChart');
let priceChart=null;
if(priceEl&&window.QUANTIX_PRICE){
 const range=Number(window.QUANTIX_SETTINGS?.range||250); const prices=window.QUANTIX_PRICE.slice(-range);
 priceChart=new Chart(priceEl,{type:'line',data:{labels:prices.map(x=>x.ts),datasets:[{label:'Close',data:prices.map(x=>Number(x.close)),tension:.2,pointRadius:0,borderWidth:2}]},options:{responsive:true,plugins:{legend:{display:false}},scales:{x:{display:false}}}});
}
const fc=document.getElementById('forecastChart');
if(fc&&window.QUANTIX_PRICE&&window.QUANTIX_FORECAST){
 const hist=window.QUANTIX_PRICE.slice(-60),m=window.QUANTIX_FORECAST,path=m.forecast_path_20d||[];
 const labels=hist.map(x=>x.ts).concat(path.map(x=>'Forecast +'+x.day));
 const actual=hist.map(x=>Number(x.close)).concat(Array(path.length).fill(null));
 const pred=Array(hist.length-1).fill(null).concat([Number(hist[hist.length-1].close)],path.map(x=>Number(x.value)));
 const low=Array(hist.length-1).fill(null).concat([Number(hist[hist.length-1].close)],path.map(x=>Number(x.low)));
 const high=Array(hist.length-1).fill(null).concat([Number(hist[hist.length-1].close)],path.map(x=>Number(x.high)));
 new Chart(fc,{type:'line',data:{labels,datasets:[{label:'Actual',data:actual,tension:.2,pointRadius:0,borderWidth:2},{label:'Forecast',data:pred,tension:.2,pointRadius:0,borderWidth:2,borderDash:[6,4]},{label:'Lower interval',data:low,tension:.2,pointRadius:0,borderWidth:1,borderDash:[3,5]},{label:'Upper interval',data:high,tension:.2,pointRadius:0,borderWidth:1,borderDash:[3,5]}]},options:{responsive:true,interaction:{mode:'index',intersect:false},plugins:{legend:{display:true}},scales:{x:{display:false}}}});
}
(function(){const symbol=window.QUANTIX_SYMBOL;if(!symbol||!window.QUANTIX_LIVE)return;let busy=false,delay=3000,timer=null;async function refresh(){if(busy||document.hidden)return;busy=true;try{const r=await fetch('api/live.php?symbol='+encodeURIComponent(symbol)+'&_='+Date.now(),{cache:'no-store'});if(!r.ok)throw new Error(r.status);const d=await r.json();if(typeof d.price==='number'){const p=document.getElementById('livePrice');if(p)p.textContent=d.price.toFixed(d.price<10?3:2);const title=document.querySelector('.chart-card .card-title span');if(title)title.textContent='Live quote • '+new Date(d.timestamp||Date.now()).toLocaleTimeString();if(priceChart&&priceChart.data.datasets[0]){const ds=priceChart.data.datasets[0];if(ds.data.length)ds.data[ds.data.length-1]=d.price;priceChart.update('none')}}delay=3000}catch(e){delay=Math.min(delay*2,30000)}finally{busy=false;clearTimeout(timer);timer=setTimeout(refresh,delay)}}refresh();document.addEventListener('visibilitychange',()=>{if(!document.hidden){delay=3000;refresh()}});window.addEventListener('beforeunload',()=>clearTimeout(timer))})();

(function(){const board=document.querySelector('.table-card tbody');if(!board)return;const symbols=[...board.querySelectorAll('a[href*="stock.php?symbol="]')].map(a=>new URL(a.href,location.href).searchParams.get('symbol')).filter(Boolean);if(!symbols.length)return;let busy=false;async function tick(){if(busy||document.hidden)return;busy=true;try{const r=await fetch('api/live.php?symbols='+encodeURIComponent(symbols.join(','))+'&_='+Date.now(),{cache:'no-store'});if(!r.ok)throw 0;const d=await r.json();for(const tr of board.querySelectorAll('tr')){const a=tr.querySelector('a[href*="stock.php?symbol="]');if(!a)continue;const sym=new URL(a.href,location.href).searchParams.get('symbol'),q=d.quotes?.[sym];if(q&&typeof q.price==='number'){const cell=tr.children[1];if(cell)cell.textContent=q.price.toFixed(q.price<10?3:2);const updated=tr.children[4];if(updated)updated.textContent=new Date(q.timestamp).toLocaleTimeString()}}}catch(e){}finally{busy=false}}tick();setInterval(tick,15000)})();
